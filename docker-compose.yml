version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /app/venv  # Prevents overriding virtualenv in container
    environment:
      - PORT=8000
    restart: always
